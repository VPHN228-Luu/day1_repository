# CLAUDE.md

## Project

Simple Node.js + Express CRUD sample app used for SPELIX Claude Code training.

## Commands

- `npm install` — install dependencies
- `npm start` — run the server (port 3000)
- `npm test` — run Jest tests

## Structure

- `server.js` — entry point (starts the HTTP server)
- `app.js` — Express app definition
- `routes/` — route handlers
- `db.js` — SQLite setup

<!-- NOTE: This CLAUDE.md is intentionally incomplete.
     Trainees improve it during the Day 2 lab (tech stack details,
     naming conventions, test location, etc.). -->
